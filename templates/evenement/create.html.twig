{% extends "base.html.twig" %}


{% block body %}
    <div>
        <div class="row">
            <div>
                <h1>Creer un Evenement</h1>
            </div>
        </div>

        <div class="row">   
            <div>
                {{form_start(form)}}
                    {{form_widget(form)}}

                    <button class="btn btn-primary" formnovalidate>
                        Creer l'evenement
                    </button>
                {{form_end(form)}}
            </div>
        </div>
    </div>
    
    
    
{% endblock %}